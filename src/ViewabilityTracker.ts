export function getVisibleRange(
  heights: Array<number[]>,
  indexer: number,
  offset: number,
  windowSize: number
): number[] {
  return [];
}
function helper(
  heights: Array<number[]>,
  indexer: number,
  offset: number,
  windowSize: number,
  min: number,
  max: number
): number {
  const mid = Math.floor((min + max) / 2);
  return 0;
}
